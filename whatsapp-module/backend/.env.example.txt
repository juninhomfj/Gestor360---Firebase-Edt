PORT=3333
NODE_ENV=production

# Firebase
# Either provide GOOGLE_APPLICATION_CREDENTIALS pointing to mounted JSON file,
# OR set FIREBASE_SERVICE_ACCOUNT_JSON as base64-encoded service account JSON.
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_PROJECT_ID=
SESSIONS_BUCKET=whatsapp-sessions

# Session encryption key (base64-encoded 32 bytes)
SESSIONS_ENC_KEY=

# API auth for module
WA_MODULE_KEY=

# Adapter/Provider
USE_OFFICIAL_WABA=false
WABA_PROVIDER_URL=
WABA_API_KEY=

# Queue options
USE_CLOUD_TASKS=true
CLOUD_TASKS_PROJECT=
CLOUD_TASKS_LOCATION=
CLOUD_TASKS_QUEUE=

# Redis fallback (if USE_CLOUD_TASKS=false)
REDIS_URL=

# Limits
DEFAULT_RATE_LIMIT=30
FRONTEND_MAX_IMPORT=500

# Optional Sentry
SENTRY_DSN=